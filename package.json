{
  "name": "text-tiler",
  "version": "1.0.0",
  "main": "index.html",
  "author": "ekelen",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "eleventy --watch --serve",
    "build": "rm -rf dist && env NODE_ENV=production npx eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.1"
  }
}
